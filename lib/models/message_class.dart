class MessageClass {
  String message;
  String accountIcon;
  bool isMe;
  DateTime sendTime;

  MessageClass({required this.message, required this.accountIcon, required this.isMe, required this.sendTime});
}